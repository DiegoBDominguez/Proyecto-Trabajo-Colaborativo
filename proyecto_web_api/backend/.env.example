# Copiar este archivo a .env y llenar los valores
# NO SUBIR .env A GITHUB (agregar a .gitignore)

# Django
DEBUG=False
SECRET_KEY=tu-clave-secreta-super-segura-aqui-cambiar-en-produccion
ALLOWED_HOSTS=tu_usuario.pythonanywhere.com,127.0.0.1,localhost

# Base de datos MySQL (PythonAnywhere)
DB_ENGINE=django.db.backends.mysql
DB_NAME=tu_usuario$nombre_db
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña_secura
DB_HOST=tu_usuario.mysql.pythonanywhere-services.com
DB_PORT=3306

# CORS
CORS_ALLOWED_ORIGINS=https://tu_usuario.pythonanywhere.com,http://localhost:4200

# Channel Layer (para notificaciones en tiempo real)
# Opciones: InMemoryChannelLayer, redis (si tienes Redis)
CHANNEL_LAYER=InMemoryChannelLayer

# Variables opcionales
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
LOG_LEVEL=INFO
